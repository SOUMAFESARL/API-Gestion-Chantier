name: Tests et déploiement cPanel

on:
  push:
    branches:
      - main

  pull_request:
    branches:
      - main

  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: cpanel-${{ github.ref }}
  cancel-in-progress: false

jobs:
  tests:
    name: Tests Django
    runs-on: ubuntu-latest
    timeout-minutes: 15

    services:
      postgres:
        image: postgres:16

        env:
          POSTGRES_USER: ccd
          POSTGRES_PASSWORD: test-only
          POSTGRES_DB: ccd_digital

        ports:
          - 5432:5432

        options: >-
          --health-cmd "pg_isready -U ccd -d ccd_digital"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 10

    env:
      DJANGO_SETTINGS_MODULE: config.settings.test
      DJANGO_SECRET_KEY: ci-only-secret-key-not-used-in-production

      POSTGRES_HOST: 127.0.0.1
      POSTGRES_PORT: 5432
      POSTGRES_USER: ccd
      POSTGRES_PASSWORD: test-only
      POSTGRES_DB: ccd_digital

    steps:
      - name: Récupérer le code
        uses: actions/checkout@v6
        with:
          persist-credentials: false

      - name: Installer Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.13"
          cache: pip
          cache-dependency-path: requirements/*.txt

      - name: Installer les dépendances
        run: |
          python -m pip install --upgrade pip
          python -m pip install -r requirements/local.txt

      - name: Vérifier la configuration Django
        run: |
          python manage.py check

      - name: Exécuter les tests backend
        run: |
          python -m pytest apps -q \
            --deselect=apps/projets/tests/test_meteo.py::test_le_referentiel_typescript_ne_derive_pas

  deploy:
    name: Déploiement FTP sur cPanel
    needs: tests

    if: github.ref == 'refs/heads/main' && github.event_name != 'pull_request' && vars.CPANEL_DEPLOY_ENABLED == 'true'

    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: Récupérer le code
        uses: actions/checkout@v6
        with:
          persist-credentials: false

      - name: Déployer les fichiers sur cPanel
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: ${{ secrets.CPANEL_FTP_SERVER }}
          username: ${{ secrets.CPANEL_FTP_USERNAME }}
          password: ${{ secrets.CPANEL_FTP_PASSWORD }}

          protocol: ftps
          port: 21

          local-dir: ./
          server-dir: /public_html/api-chantier/

          exclude: |
            **/.git*
            **/.git*/**
            **/.github/**
            **/__pycache__/**
            **/*.pyc
            **/.env
            **/.env.*
            **/venv/**
            **/env/**
            **/.venv/**
            **/node_modules/**
            **/tests/**
            **/.pytest_cache/**
            **/media/**
            **/staticfiles/**
            **/*.sqlite3

      - name: Vérifier la disponibilité de l’API
        if: vars.CPANEL_HEALTH_URL != ''
        shell: bash
        run: |
          set -euo pipefail

          for attempt in {1..12}; do
            if curl \
              --fail \
              --silent \
              --show-error \
              --connect-timeout 10 \
              --max-time 20 \
              "${{ vars.CPANEL_HEALTH_URL }}" \
              -o /tmp/api-health.json; then

              python - <<'PY'
          import json

          with open("/tmp/api-health.json", "r", encoding="utf-8") as file:
              response = json.load(file)

          assert response.get("statut") == "ok", response
          PY

              echo "API disponible."
              exit 0
            fi

            echo "API indisponible. Nouvelle tentative dans 5 secondes..."
            sleep 5
          done

          echo "La vérification HTTPS a échoué."
          exit 1