from django.apps import AppConfig


class TiersConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.tiers"
    verbose_name = "Référentiel unique des acteurs externes (décision H3 du MCD)"
